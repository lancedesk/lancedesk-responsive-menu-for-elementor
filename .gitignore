# Local WordPress.org SVN working copy (same name as repo folder; do not commit to Git).
/lancedesk-responsive-menu-for-elementor/

# Deployment/sync scripts (local use only).
/scripts/

# Internal migration docs and checklists (local planning).
/docs/
